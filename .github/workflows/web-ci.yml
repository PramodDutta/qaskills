name: Web CI

on:
  push:
    branches: [main]
    paths:
      - 'packages/web/**'
      - 'packages/shared/**'
  pull_request:
    branches: [main]
    paths:
      - 'packages/web/**'
      - 'packages/shared/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm --filter @qaskills/shared build && pnpm --filter @qaskills/web lint

      - name: Type check
        run: pnpm --filter @qaskills/web lint

      - name: Build
        run: pnpm --filter @qaskills/web build
        env:
          QASKILLS_DISABLE_AUTH: '1'
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Unit tests
        run: pnpm --filter @qaskills/web test:unit

      - name: Install Playwright Chromium
        run: pnpm --filter @qaskills/web exec playwright install --with-deps chromium

      - name: Post-flow browser tests
        run: pnpm --filter @qaskills/web test:e2e
